---
import {
  type Size,
  type LinkStyle,
  sizeClasses,
  baseClasses,
  linkStyleClasses,
} from "./buttonStyles";

interface Props {
  href: string;
  size?: Size;
  block?: boolean;
  style?: LinkStyle;
  class?: string;
  [x: string]: any;
}

const {
  href,
  block,
  size = "lg",
  style = "primary",
  class: className,
  ...rest
} = Astro.props;
---

<a
  href={href}
  {...rest}
  class:list={[
    baseClasses,
    block && "w-full",
    sizeClasses[size],
    linkStyleClasses[style],
    className,
  ]}
  ><slot />
</a>
