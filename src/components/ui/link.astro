---
import {
  sizes,
  styles,
  baseClasses,
  type Size,
  type Style,
} from "./shared-styles";

interface Props {
  href: string;
  size?: Size;
  block?: boolean;
  style?: Style;
  class?: string;
  [x: string]: any;
}

const {
  href,
  block,
  size = "lg",
  style = "primary",
  class: className,
  ...rest
} = Astro.props;
---

<a
  href={href}
  {...rest}
  class:list={[
    baseClasses,
    block && "w-full",
    sizes[size],
    styles[style],
    className,
  ]}
  ><slot />
</a>
